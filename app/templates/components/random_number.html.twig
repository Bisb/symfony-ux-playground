<div{{ attributes }}>
  <input type="number" data-model="debounce(500)|max">
  {{ max }}

  <strong>{{ this.randomNumber }}</strong>

  <button data-action="live#$render">
    Generate a new number
  </button>
</div>
